<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gaoxi.mapper.PetMapper">
    <!--配置/实现public List<Pet> getPetByUserId(Integer userId)
     完成的思路和前面大体相同
     -->
    <resultMap id="PetResultMap" type="Pet">
        <id property="id" column="id"/>
        <result property="nickname" column="nickname"/>
        <result property="userId" column="user_id"/>
        <association property="user" column="user_id" select="com.gaoxi.mapper.UserOfPetsMapper.getUserById"/>
    </resultMap>
    <select id="getPetByUserId" parameterType="Integer" resultMap="PetResultMap">
        SELECT * FROM `mybatis_pet` WHERE `user_id` = #{userId}
    </select>
    <!--
     1.配置/实现public Pet getPetById(Integer id)
     2.复用已有的resultMap，这就是resultMap的好处
     -->
    <select id="getPetById" parameterType="Integer" resultMap="PetResultMap">
        SELECT * FROM `mybatis_pet` WHERE `id` = #{id}
    </select>
</mapper>